<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
</head>
<body>
    <h1>{{title}}</h1>
    <form action="/droplets" method="post">
        <label>
            Url:
            <input type="text" name="url" />
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <button type="submit">Submit</button>
        </label>
    </form>
</body>
</html>